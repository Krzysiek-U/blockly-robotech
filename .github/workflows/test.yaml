name: Build and Deploy Blockly
on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: write
  pages: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          # Pętla próbująca odblokować i zainstalować narzędzia (naprawa błędu 100)
          for i in {1..5}; do
            sudo rm -rf /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock
            sudo apt-get update -y && \
            sudo apt-get install -y --no-install-recommends subversion python2-minimal default-jre && \
            break || \
            (echo "System zablokowany, próba $i z 5 za 10 sekund..." && sleep 10)
          done
          # Ustawienie aliasu dla Pythona 2
          sudo ln -sf /usr/bin/python2 /usr/bin/python

      - name: Build Maze
        run: |
          # Pobieranie bibliotek (ignorujemy błędy sieciowe SVN)
          make deps || echo "Problemy z SVN, ale próbuję dalej..."
          # Kompilacja Labiryntu w wersji polskiej
          make maze-pl

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./appengine
          force_orphan: true
